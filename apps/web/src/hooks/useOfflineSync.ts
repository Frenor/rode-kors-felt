/**
 * @deprecated Incident offline queue has been removed.
 * This hook is a no-op kept only so existing imports don't break during migration.
 * Remove usages and then delete this file.
 */

export function useOfflineSync() {
  // no-op
}

